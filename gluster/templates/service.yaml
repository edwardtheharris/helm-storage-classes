apiVersion: v1
kind: Service
metadata:
  name: {{ include "gluster.fullname" . }}
  labels:
    {{- include "gluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gluster.selectorLabels" . | nindent 4 }}
