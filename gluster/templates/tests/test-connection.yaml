apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gluster.fullname" . }}-test-connection"
  labels:
    {{- include "gluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
